// Netlify frontend configuration.
// Set this to the public HTTPS URL of the Go backend, for example:
// window.BINGO_CONFIG = { backendUrl: "https://your-bingo-backend.example.com" };
// Leave it empty for local same-origin development with `go run .`.
window.BINGO_CONFIG = {
  backendUrl: ""
};

